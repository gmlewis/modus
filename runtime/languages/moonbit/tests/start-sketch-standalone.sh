#!/bin/bash -ex
# According to: https://sketch.dev/docs/deployment-modes
# "standalone" means to use your local Docker Desktop and your own
# `ANTHROPIC_API_KEY` that must be set before running this script.
sketch -skaband-addr=""
