package modus:system

// Interface for system logging
interface console {
    // Log a message with specified level
    log-message: func(level: string, message: string)
}

// Define the world block
world system-world {
    import console
}
